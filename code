Begin

initialize variables
int as num1, num2
int as sum, diff, prod

float as quot

display "enter your first number"
input your first number

display "enter your second number"
input your second number

compute the sum of num1 and num 2
compute the diff of num1 and num2
compute the prod of num1 and num2
compute the quot of num1 and num2

display the sum and result
display the diff and result
display the prod and result
display the quot and result

End
