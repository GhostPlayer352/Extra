{"Username":"seth_ka123","UserId":5347040425,"Message":"local oldHttpGet\noldHttpGet = hookfunction(game.HttpGet, function(self, url, ...)\n    if url:find(\"status\") or url:find(\"check\") then\n        return \"true\"\n    end\n    return oldHttpGet(self, url, ...)\nend)local oldHttpGet\noldHttpGet = hookfunction(game.HttpGet, function(self, url, ...)\n    if url:find(\"status\") or url:find(\"check\") then\n        return \"true\"\n    end\n    return oldHttpGet(self, url, ...)\nend)local oldHttpGet\noldHttpGet = hookfunction(game.HttpGet, function(self, url, ...)\n    if url:find(\"status\") or url:find(\"check\") then\n        return \"true\"\n    end\n    return oldHttpGet(self, url, ...)\nend)local oldHttpGet\noldHttpGet = hookfunction(game.HttpGet, function(self, url, ...)\n    if url:find(\"status\") or url:find(\"check\") then\n        return \"true\"\n    end\n    return oldHttpGet(self, url, ...)\nend)local oldHttpGet\noldHttpGet = hookfunction(game.HttpGet, function(self, url, ...)\n    if url:find(\"status\") or url:find(\"check\") then\n        return \"true\"\n    end\n    return oldHttpGet(self, url, ...)\nend)local oldHttpGet\noldHttpGet = hookfunction(game.HttpGet, function(self, url, ...)\n    if url:find(\"status\") or url:find(\"check\") then\n        return \"true\"\n    end\n    return oldHttpGet(self, url, ...)\nend)"}